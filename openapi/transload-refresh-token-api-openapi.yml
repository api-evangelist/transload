openapi: 3.2.0
info:
  title: Pipeline Backend Refresh Token API
  version: 0.1.0
security:
- bearerAuth: []
tags:
- name: Refresh Token
paths:
  /refresh-token:
    post:
      responses:
        '200':
          description: Default Response
      tags:
      - Refresh Token
      summary: Create refresh token
      x-summary-source: derived
      operationId: postRefreshToken
      x-operation-id-source: derived
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
