openapi: 3.2.0
info:
  title: Pipeline Backend Handling Units API
  version: 0.1.0
security:
- bearerAuth: []
tags:
- name: Handling Units
paths:
  /v1/handling-units/{handlingUnit}/latest-result:
    get:
      parameters:
      - schema:
          type: string
          format: uuid
        in: query
        name: customerId
        required: false
      - schema:
          type: string
          minLength: 1
        in: path
        name: handlingUnit
        required: true
      responses:
        '200':
          description: Default Response
      tags:
      - Handling Units
      summary: Get handling units by handling unit latest result
      x-summary-source: derived
      operationId: getV1HandlingUnitsByHandlingUnitLatestResult
      x-operation-id-source: derived
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
