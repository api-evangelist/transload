openapi: 3.2.0
info:
  title: Pipeline Backend Measurement API
  version: 0.1.0
security:
- bearerAuth: []
tags:
- name: Measurement
paths:
  /v1/measurement/results:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                run_id:
                  type: string
                  minLength: 1
                status:
                  type: string
                  enum:
                  - success
                  - error
                measurements:
                  type: array
                  items:
                    type: object
                    properties:
                      image_name:
                        type: string
                      camera:
                        type: string
                      length_cm:
                        type: number
                      width_cm:
                        type: number
                      height_cm:
                        type: number
                    required:
                    - image_name
                    - camera
                    - length_cm
                    - width_cm
                    - height_cm
                  default: []
                expected_scans:
                  type:
                  - integer
                  - 'null'
                  default: 0
                output_uris:
                  type: object
                  properties:
                    measurements_csv:
                      type:
                      - string
                      - 'null'
                    ply_files:
                      type: array
                      items:
                        type: string
                    ply_downsampled_files:
                      type: array
                      items:
                        type: string
                    obb_projection_json_files:
                      type: array
                      items:
                        type: string
                error:
                  type:
                  - string
                  - 'null'
                error_type:
                  type:
                  - string
                  - 'null'
                  enum:
                  - inference_oom
                  - inference_failed
                  - mask_missing
                  - image_download_failed
                  - config_invalid
                  - upload_failed
              required:
              - run_id
              - status
      responses:
        '200':
          description: Default Response
      tags:
      - Measurement
      summary: Create measurement results
      x-summary-source: derived
      operationId: postV1MeasurementResults
      x-operation-id-source: derived
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
